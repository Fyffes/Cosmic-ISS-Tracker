export default {
  plugins: {
    // Ermöglicht die Nutzung von @import in CSS-Dateien
    'postcss-import': {},
    // Lädt Tailwind CSS
    'tailwindcss': {},
    // Fügt automatisch Vendor-Präfixe hinzu (z.B. -webkit-)
    'autoprefixer': {},
  },
}